/**
 * Autosave.js - Disabled as requested
 * (Previously: Professional Offline Drafts for Lenoir-Mec)
 */

document.addEventListener('DOMContentLoaded', () => {
    // Fonction désactivée pour éviter les messages intrusifs
    console.log("Autosave: Function disabled by admin request.");
});

function clearAutosaveDraft() {
    // Keep function signature for compatibility but it's now a no-op or just clears everything
    localStorage.clear(); 
}
